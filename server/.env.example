MONGODB_URI=mongodb+srv://zakariadouami1999_db_user:4kkN7laNbPzoPXUK@cluster0.rhdy3yh.mongodb.net/car-rental?retryWrites=true&w=majority&appName=Cluster0
# Must be ≥32 characters in production (server refuses to start otherwise)
JWT_SECRET=change-this-to-a-long-random-secret-at-least-32-chars
CLIENT_URL=https://car-rentelzak.vercel.app
PORT=3000
# NODE_ENV=production
# TRUST_PROXY=true

# ImageKit (required for car/profile image uploads)
IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_PRIVATE_KEY=
IMAGEKIT_URL_ENDPOINT=

# Optional agency admin seed script
ADMIN_EMAIL=admin@hdncar.com
ADMIN_PASSWORD=Admin123!
ADMIN_NAME=Admin

# Platform Super Admin (dedicated /superadmin login — never share with agency admins)
# Seed with: npm run seed:superadmin
SUPERADMIN_EMAIL=superadmin@hdncar.com
SUPERADMIN_PASSWORD=SuperAdmin123!
SUPERADMIN_NAME=Super Admin

# Trial / license (single agency — not multi-tenant)
# New admins get a trial of TRIAL_DAYS. Manage via Super Admin panel or:
# npm run license:status|activate|extend|trial|expire
TRIAL_DAYS=7

# WhatsApp notifications on new reservations (Meta Cloud API or Twilio)
WHATSAPP_ENABLED=false
WHATSAPP_PROVIDER=meta
# Required when WHATSAPP_ENABLED=true — no hardcoded fallback number
WHATSAPP_TO=
WHATSAPP_CURRENCY=MAD
# Meta WhatsApp Cloud API (recommended for production)
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_API_VERSION=v21.0
# Optional: approved template for business-initiated messages
WHATSAPP_TEMPLATE_NAME=
WHATSAPP_TEMPLATE_LANG=en
# Twilio alternative (set WHATSAPP_PROVIDER=twilio)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# Agency branding (emails / PDFs)
AGENCY_NAME=HDN Car Rental
CURRENCY=MAD
API_PUBLIC_URL=https://carrentelzak.onrender.com
COMPLETION_TOKEN_DAYS=7
DEPOSIT_PERCENT=30

# Email (SMTP) — REQUIRED for completion & final confirmation emails
# Gmail: enable 2FA → create App Password → paste WITHOUT needing spaces
# SMTP_FROM should use the same address as SMTP_USER (Gmail rejects mismatched From)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false

SMTP_USER=b292b4001@smtp-brevo.com
SMTP_PASS=xsmtpsib-b408f28f6bddc25d89444706370a06e329a01f061cb3a73a60113d60f570bca4-yjdx5DrfIbFxNAaO

SMTP_FROM="HDN Car Rental <creaticodedev@gmail.com>"
AGENCY_NAME=HDN Car Rental
# SMTP_DEBUG=true
# SMTP_ALLOW_CUSTOM_FROM=false

# Online payments — Stripe required in production (demo is blocked unless ALLOW_DEMO_PAYMENT=true)
PAYMENT_MODE=demo
PAYMENT_CURRENCY=mad
STRIPE_SECRET_KEY=
# ALLOW_DEMO_PAYMENT=true
# Break-glass: allow owner to force ready_for_pickup without completion (default off)
# ALLOW_FORCE_BOOKING_STATUS=true
